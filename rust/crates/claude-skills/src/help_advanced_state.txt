  Native orchestration tooling now includes route planning from the working brief, explicit tasks, and requirement state instead of raw keywords alone, artifact-first resume-status reporting for long-running work, fail-closed non-trivial run starts that require a working brief plus completion ledger, and machine-readable execution traces that record which specialist lanes were actually used.
  Phase 3 direct native memory tooling now includes scope resolution, native SYSTEM_MAP refresh, a canonical project-scoped global SYSTEM_MAP target, working-brief tracking, completion-gate tracking, agent-registry management with reuse planning and wait planning, research-cache operations, maintenance flows, loop-guard tracking, agent-packet generation, and memory reporting through the claude-skills binary.
  MemoriesV2 is the writable global second-layer memory store under ~/.claude, split into global, workspace, workstream, and optional lane files, and the native memory workflow now keeps it synchronized; native memoriesv2 also assembles deterministic scoped retrieval packets with receipts, keeps an append-only ledger plus graph edges for durable task and decision state, and can consolidate scoped artifacts into reusable workstream and workspace summaries without relying on MCP.
